import React from 'react';
import LoginCard from './Login/patterns/login-card';

const LoginPage = () => {
  return (
    <div className="w-full h-screen bg-amber-400 flex items-center justify-center">
      <LoginCard />
    </div>
  );
};

export default LoginPage;
